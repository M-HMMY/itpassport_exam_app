# CLAUDE.md

このファイルは、このリポジトリで作業するときに最初に読む前提です。

## このアプリは何か

ITパスポート試験（IPA）の学習アプリ。**教本を軸にした学習アプリ**であって、問題集アプリではありません。

土台は `fe_exam_app`（基本情報技術者試験）→ `e_exam_app`（E資格）と引き継いできたものです。画面・記法・運用の作法はそちらと共通なので、迷ったら向こうの実装を見れば答えがあります。

## 最優先の方針

**「通読できる教本」が最優先の要件です。** 演習機能より教本の質を優先してください。

その上で、ITパスポートには他の 2 つと決定的に違う事情があります。

**読者は IT の知識がゼロの人です。** 学生・事務職・営業職・これから就職する人。「サーバ」「プロトコル」「アルゴリズム」という言葉を一度も使ったことがない人が読みます。**姉妹アプリの文章をそのまま持ってくると、確実に難しすぎます。**

書くときの基準は次のとおりです。

- **専門用語は、初めて出したその場で必ず言い換える。** 「後で説明します」は使わない
- **身近なたとえを最初に置く。** 郵便、電話、引き出し、レシピ、文化祭の準備。抽象的な定義はそのあと
- **1 文を短く。** 修飾を重ねない
- **カタカナ語には、なぜその名前なのかを添える。** 「キャッシュ（cache＝隠し場所。よく使うものを手前に置いておく）」
- **計算は「なぜその式になるか」から入る。** 公式の暗記を求めない

判断に迷ったら「**中学生に説明するつもりで書けているか**」を基準にしてください。

## 試験の形（教本の前提）

| 項目 | 内容 |
| --- | --- |
| 形式 | 四肢択一の小問のみ（CBT）。記述も実技もない |
| 問題数・時間 | 100 問を 120 分。うち 8 問程度は採点対象外 |
| 受験条件 | なし。誰でも受けられ、ほぼ毎日どこかの会場で実施 |
| 出題数 | ストラテジ系 35・マネジメント系 20・テクノロジ系 45 問 程度 |
| 合格基準 | 総合 600 点以上（1000 点満点）**かつ、3 分野それぞれ 300 点以上** |

**分野別基準があるので、苦手分野を捨てられません。** この配点構造がアプリの設計の前提です（模試は本番の分野比率で出題し、分野別評価点も表示します）。

要項は改訂されます。断定的に書かず、「最新の情報は IPA の公式サイトで確認」と促してください。

## 変えないこと

- **ランタイム依存は React だけ。** ルータ・Markdown・数式・図はすべて自前。ライブラリを足す前に、本当に必要か検討する
- **外部通信をしない。** 学習記録は localStorage のみ。サーバもアカウントもない
- **オフラインで動く。** PWA として成立させる
- TypeScript は strict（未使用変数もエラー）

## 節の書き方

本文は Markdown サブセットです。1 つの節は次の骨格で書きます。

```
# ざっくり言うと          ← 日常語で「何の話か」。用語を使わずに書く
（本論。見出し・表・図・たとえ話）
> **試験のポイント** …     ← 暗記すべき事項
> **よくある勘違い** …     ← 先回りして潰す誤解
# この節のまとめ           ← 箇条書き 5〜6 個
```quiz                    ← 一問一答（採点も記録もしない）
問い :: 答え
```
```

「この節のまとめ」「試験のポイント」「よくある勘違い」の 3 つは、[src/lib/digest.ts](src/lib/digest.ts) が自動で抜き出して**直前チェックシート**に並べます。**書式を崩すと拾われません。**

### 節の ID

`<categoryId>-<連番>` にします（例：`t-sec-1`、`s-corp-3`）。ただし入門編だけは `i-1`〜`i-4` で、**`i-1` は最初の節、`i-4` は用語ミニ辞典**という前提が [src/pages/Textbook.tsx](src/pages/Textbook.tsx) に埋まっています。

### 使える記法

| 記法 | 用途 |
| --- | --- |
| `# ## ###` | 見出し |
| `- ` `1. ` | 箇条書き（2 スペースで 1 段ネスト） |
| `\|` の表 | 表 |
| `> ` | 引用（＝ポイント枠） |
| `**強調**` | 太字 |
| `` `コード` `` | 等幅 |
| `[文字](textbook/t-sec-1)` | アプリ内リンク（外部 URL は不可） |
| `$...$` | 行内数式（稼働率の式などに使う。多用しない） |
| ` ```math ` | 別行立ての数式 |
| ` ```diagram:種類 ` | 図（flow / stack / tree / matrix / cycle / seq / bits / compare） |
| ` ```widget:id ` | 対話ウィジェット |
| ` ```quiz ` | 一問一答（`問い :: 答え`） |

**図は積極的に使ってください。** IT が初めての読者には、文章より図の方が速く届きます。`compare` は「1 行 1 セル、偶数行が左・奇数行が右」で書きます（要素が奇数だと `npm run check` がエラーにします）。図の中では `**強調**` が効かないので書かないこと。

数式は使えますが、**この試験では出番が少ない**です。稼働率や損益分岐点のように式で書いた方が分かりやすいときだけにしてください。使える記号は [src/lib/mathSymbols.ts](src/lib/mathSymbols.ts) にあります。

### バックスラッシュの落とし穴（重要）

本文は TypeScript の**テンプレートリテラル**の中にあります。エスケープが必要なのは次の 3 つです。

| 書きたいもの | ファイルに書く形 |
| --- | --- |
| `\times` | `\\times` |
| バックティック | バックスラッシュ + バックティック |
| `${` | `\${` |

さらに、**bash のヒアドキュメント（`cat > file <<'EOF'`）はバックスラッシュを勝手に落とします。** この環境で何度も壊しました。バックスラッシュを含むファイルを書くときは、

- **Write ツールを使う**（いちばん確実）、または
- **[scripts/emit_textbook.py](scripts/emit_textbook.py) を使う**（Markdown を Python の raw 文字列で書き、エスケープしてから `.ts` に書き出す）

のどちらかにしてください。教本をまとめて書くときは後者が便利です。

## 問題の書き方

- **`sectionId` は必ず付ける。** 解説から教本へ戻る導線と、節ごとの出題に使う
- 選択肢は 4 つ。**正解の位置は散らす**（0 ばかりにしない）
- 解説では、正解の理由だけでなく**誤答がなぜ誤りか**にも触れる
- 1 節あたり 4 問以上を目安にする
- 通常の文字列リテラルでもバックスラッシュは 2 つ重ねる

## 作業の作法

### 必ず通すもの

```
npm run check   # データの整合性 + 実際に描いてみる検査
npx tsc --noEmit
npm run build
```

`npm run check` は「型では防げない食い違い」を捕まえます。ID 重複、参照切れ、記法の崩れ、図の書式、リンクの飛び先、数式のエスケープ漏れ、ドリルの選択肢、そして**実際に React で描画して NaN や崩れが出ないか**まで見ます。**新しい不整合の型を見つけたら、直すついでに [scripts/check.ts](scripts/check.ts) へ検査を足してください。**

### 内容の正しさ

**書いた本人が読み返しても間違いは見つかりません。** `fe_exam_app` では新規に書いた約 7 万字を独立した 5 つのエージェントに読ませて **16 件の誤り**（うち 4 件は重大な事実誤り）を発見しました。自己レビューでは 1 件も見つかっていません。`e_exam_app` でも、確認問題の重複 8 件はレビューで初めて出てきました。

まとまった量を書いたら、**書いた人とは別の目で検証する工程を必ず入れてください。**

ITパスポート特有の検証観点として、**「初心者に通じるか」も見てください。** 事実として正しくても、用語を説明なしで使っていれば失格です。

このアプリでも一度やりました。全 52 節を独立した 5 つのレビューに読ませ、**重大な事実誤り 30 件・その他 60 件超**が出ています。自己レビューでは 1 件も出ていません。実際に見つかった型は次のとおりです。**同じ型は繰り返し起きるので、書くときも直すときも意識してください。**

- **法改正の年をまたいだ誤り** … 映画の著作物を「2018 年に 50 年から 70 年へ延長」の側に入れていた（映画はそれ以前から 70 年）
- **「まとめて言い切る」ことで生まれる誤り** … 産業財産権 4 つを一律に「審査を経て登録」としたが、実用新案に実体審査はない
- **節をまたいだ食い違い** … K・M・G を、ある節では 1,024 倍、別の節では 1,000 倍として扱っていた
- **本文と図の食い違い** … PPM の横軸が、本文は相対的市場占有率、図は市場占有率
- **例と一般論の食い違い** … 二分探索の候補数を「7→4→2→1」と書いたが、直前の例では 7→3→1
- **図の名前と中身の食い違い** … アローダイアグラムと呼びながら、実際はプレシデンスダイアグラムを描いていた
- **アプリの実装との食い違い** … 教本の「間違えた問題だけが復習に回る」という説明が、実装（正解も登録される）と違っていた
- **説明なしの初出** … ソフトウェア、メモリ、ネットワーク、ブラウザなどが定義より前に出ていた

逆に、**レビュー側が範囲外の指摘をしてくる**こともあります。ITパスポートに出ない OSI 参照モデルの 7 層や IPv4 のクラス分けを「重大な欠落」として挙げてきました。プロンプトでこちらが例示した語がそのまま誘導になっていました。**採否はこちらで判断してください。**

### 公開のルール

公開リポジトリにする前提です。

- **コミットの署名は GitHub の noreply アドレスを使う。** 実メールアドレスはコミット履歴に永久に残ります
- 個人情報・ローカルパス・API キーを含めない

## いまの状態

| 項目 | 状況 |
| --- | --- |
| アプリの仕組み | 完成。型チェック・ビルド・整合性チェックすべて通る |
| 教本 | **全 52 節・約 164,200 字。全章そろい、通しレビューと修正も一巡した** |
| 確認問題 | **0 問**。ここが最大の穴 |
| 計算ドリル | 0 種類 |
| 対話ウィジェット | 0 個 |

章の一覧は次のとおりです。実際の節数と字数は `npm run check` が教えてくれます。

| ファイル | 章 | 節 |
| --- | --- | --- |
| `intro.ts` | はじめに | i-1〜i-4 |
| `strategy.ts` | 企業と法務 | s-corp-1〜5 |
| `strategy-biz.ts` | 経営戦略とビジネス / システム戦略と企画 | s-biz-1〜5・s-sys-1〜2 |
| `management.ts` | 開発技術 / プロジェクトマネジメント / サービスマネジメント | m-dev-1〜3・m-pm-1〜2・m-sm-1〜3 |
| `tech-basic.ts` | 基礎理論 / アルゴリズムとプログラミング | t-basic-1〜4・t-algo-1〜3 |
| `tech-comp.ts` | コンピュータの構成 / システムとソフトウェア | t-comp-1〜3・t-sys-1〜3 |
| `tech-media-db.ts` | 情報デザインとメディア / データベース | t-media-1〜3・t-db-1〜3 |
| `tech-nw.ts` | ネットワーク | t-nw-1〜4 |
| `tech-sec.ts` | 情報セキュリティ | t-sec-1〜5 |

教本のファイルは「章ごと」ではなく**「まとめて書く単位ごと」**に分けてあります。複数のエージェントを並行して走らせるとき、1 ファイル 1 担当にすれば衝突しないためです。

## 章を書かせるやり方

**Codex CLI が導入済みです**（`npm install -g @openai/codex`、ChatGPT ログイン済み）。教本の大半はこれで書きました。

```bash
codex exec --cd "C:/Dev/itpassport_exam_app" --sandbox workspace-write --color never -o out.txt - < scripts/prompts/03-tech-sec.md
```

実際に使ったプロンプトは [scripts/prompts/](scripts/prompts/) に全部残してあります。**残りの章や確認問題を書かせるときは、そこの README を読んでひな形にしてください。** 何をプロンプトに入れると品質が上がるか（間違えやすい事実の名指し、リンクしてよい節 id の全列挙など）を書いてあります。

依頼側のコンテキストを食わないよう、`-o` で最終メッセージだけをファイルに落とすのが要点です。

## 次にやること

優先順に並べています。

1. **確認問題を書く** … **ここが最大の穴です。**本番 100 問なので、模試を回すなら 300 問以上ほしい。1 節あたり 4 問以上を目安に。**分野別の足切りがあるので、どの分野も薄くしないこと。**受け皿は `src/data/questions/` に空の配列として用意してあります（`strategy.ts` / `management.ts` / `technology.ts` の 3 ファイル）。テクノロジ系は 28 節分あるので、1 ファイル 1 担当のまま並行させるならファイルを割る必要があります
2. **計算ドリル** … 2 進数変換、記憶容量、稼働率、損益分岐点、期待値、通信時間
3. **対話ウィジェット** … [src/pages/Tools.tsx](src/pages/Tools.tsx) の `GROUPS` が計画表を兼ねています。`src/components/widgets/<id>.tsx` を置くだけで自動登録されます
4. **まだ薄い章を厚くする** … 通しレビューの回で `strategy-biz` と `management` を増補しましたが、それでも 1 節あたりで見るといちばん薄いのはこの 2 つ（約 2,600〜2,800 字/節）と `tech-comp`（約 2,700 字/節）です。いちばん厚い `tech-nw` は約 4,600 字/節あります
5. **確認問題を書き終えたら、もう一度通しレビュー** … 問題文と解説にも同じ検証が要ります。教本のときのプロンプトは [scripts/prompts/90-review.md](scripts/prompts/90-review.md) にあります
6. **ブラウザでの通し確認** … `launch.cmd` で開発サーバが立ちます。`npm run check` は描画崩れまで見ますが、**見た目そのものは人が見るしかありません**
