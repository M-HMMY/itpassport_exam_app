import type { TextbookSection } from '../../types';

/**
 * この章はこれから書く。書き方は CLAUDE.md を見ること。
 * 節の骨格：ざっくり言うと → 本論（図・表・たとえ話）→ この節のまとめ → 一問一答（quiz）。
 * 「試験のポイント」「よくある勘違い」の引用ブロックは直前チェックシートに自動で集まる。
 */
export const techSec: TextbookSection[] = [];
